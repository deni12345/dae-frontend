{
  "version": 2,
  "installCommand": "corepack use yarn@4.9.2 && yarn install",
  "buildCommand": "yarn build",
  "builds": [
    {
      "src": "server/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server/index.js"
    }
  ]
}
